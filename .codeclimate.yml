engines:
  eslint:
    enabled: true
    channel: "eslint-3"
    config:
      config: .eslintrc.json

ratings:
  paths:
  - "src/**/*.js"

ecmaFeatures:
  modules: true

env:
  amd: true
  browser: true
  es6: true
  jquery: true
  node: true
